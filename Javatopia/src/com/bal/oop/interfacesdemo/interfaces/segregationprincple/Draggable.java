package com.bal.oop.interfacesdemo.interfaces.segregationprincple;

/*
prefix dragg > able, or resiz > able
 */
public interface Draggable {
    /*
    we have a warning  because we should declare each interface or each class in a separate file.
    shortcut for separating
    alt + enter > move this class
     */
    void drag();
}
