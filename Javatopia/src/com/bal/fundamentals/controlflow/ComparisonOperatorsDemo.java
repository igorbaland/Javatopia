package com.bal.fundamentals.controlflow;

public class ComparisonOperatorsDemo {

    public void demo() {
        //comparison operators > compare primitive values
        //==, !=, >, >=, <, <=

        int x = 1;
        int y = 1;
        // = > set, == > compare
        // == expression > boolean (true/false) expression
        System.out.println(x == y);
    }
}
