package com.bal.oop.thethirdprincipal.inheritance.accessmodifier;

public class AccessModifierDemo {
    /*
    public > are accessible from anywhere
    protected > is like public, but only in own package & parents class accessible from anywhere
    default > package private, accessible anywhere in this package, but private outside this package
    private > only in own class. We use them to hide the implementation detail of a class. So we can change the
    implementation
     */

    public void demo() {

    }
}
