package com.bal.oop.interfacesdemo.interfaces;

public class InterfaceDemo {
    public void demo(){
        /*
        We cannot instantiate this interface like a class. In other words, we can not declare a variable and set a new
        instance of the TaxCalculator. Because this is an interface because this is a contract, not a concrete class
        implementation, it doesn't have any code.
        So how are we going to use this?
        Back to our class TaxCalculator2023 and implements an interface
         */
    }
}
